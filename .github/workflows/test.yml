---
name: Test

on: # yamllint disable-line rule:truthy
  push:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Lint
        run: make lint

      - name: Test
        run: make test
